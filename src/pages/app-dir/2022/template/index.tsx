import Layout from "../../../../components/layout/layout";

export default function AppTemplate({ ip, headers }) {
  return (
    <Layout pageTitle="Your IP">
      <h1>App Template</h1>
    </Layout>
  );
}
